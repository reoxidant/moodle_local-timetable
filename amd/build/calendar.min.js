/**
 * Used to get schedule of discipline
 *
 * @package timetable
 */

define(
    [
        'jquery',
        'core/ajax',
        'core/custom_interaction_events',
        'core/notification'
    ], function(
        $,
        Ajax,
        CustomEvents,
        Notification
    ){

        var registerEventListeners = function() {
            var calendar_table = $('calendar_table');
            var input_date = calendar_table.find('input');
        }

        return{
            init:function () {
                $(window).ready(function() {
                    // console.log('ready');
                    registerEventListeners();
                });
            }
        }
    }
);